<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/layout_fill_wrap"
    android:background="@drawable/footbar_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal" >

    <RadioButton
        android:id="@+id/message_bottom_first_btn"
        style="@style/tab_item"
        android:checked="true"
        android:drawableTop="@drawable/tab_home_selector"
        android:text="@string/tab_home" />

    <RadioButton
        android:id="@+id/message_bottom_second_btn"
        style="@style/tab_item"
        android:drawableTop="@drawable/tab_sort_selector"
        android:text="@string/tab_sort" />

    <FrameLayout
        android:layout_width="0.0dip"
        android:layout_height="wrap_content"
        android:layout_weight="1.0" >

        <LinearLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:gravity="bottom|center" >

            <RadioButton
                android:id="@+id/message_bottom_third_btn"
                style="@style/tab_item"
                android:drawableTop="@drawable/tab_bag_selector"
                android:text="@string/tab_bag" />
        </LinearLayout>

        <TextView
            android:id="@+id/tab_unread_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|right"
            android:layout_marginRight="5dip"
            android:background="@drawable/tab_unread_bg"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="10.0dip"
            android:visibility="invisible" />
    </FrameLayout>

    <RadioButton
        android:id="@+id/message_bottom_fourth_btn"
        style="@style/tab_item"
        android:drawableTop="@drawable/tab_more_selector"
        android:text="@string/tab_more" />

</LinearLayout>